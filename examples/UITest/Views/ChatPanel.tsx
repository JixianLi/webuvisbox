// ABOUTME: Right-side placeholder panel for the UI test scenario.
// ABOUTME: Drop new rendering components in here as they're being built.

import Panel from "@/Panels/Panel";

export default function ChatPanel() {
    return (
        <Panel panelName="Chat">
            <div>Chat placeholder</div>
        </Panel>
    );
}
