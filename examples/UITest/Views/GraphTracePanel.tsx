// ABOUTME: Left-side placeholder panel for the UI test scenario.
// ABOUTME: Drop new rendering components in here as they're being built.

import Panel from "@/Panels/Panel";

export default function GraphTracePanel() {
    return (
        <Panel panelName="Graph Trace">
            <div>Graph trace placeholder</div>
        </Panel>
    );
}
